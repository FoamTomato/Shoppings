package com.sybinal.shop.config;

//@Configuration
/*@WebAppConfiguration
@EnableSwagger2
@EnableWebMvc*/
public class swaggerConfig {

	/**
     * 配置了swagger的Docket的bean实例
     * @return
     */
    /*@Bean
    public Docket docket(Environment environment){
        *//**
         * 获取返回dev的环境
         *//*
        //获取项目的环境
        return new Docket(DocumentationType.SWAGGER_2)
                .apiInfo(apinfo())
                .groupName("Foam")
                .enable(true)//关闭swagger 浏览器不能访问
                .select()
                //RequestHandlerSelectors.配置要扫描接口的方式
                //basePackage 指定要扫描的包
                //any() 扫描全部
                //none() 不扫描
                //withClassAnnotation  扫描类上的注解
                //apis(RequestHandlerSelectors.withClassAnnotation(GetMapping.class))
                //withMethodAnnotation  扫描方法上的注解
                //.apis(RequestHandlerSelectors.basePackage("com.swag.swagger"))
                //paths() 过滤路径
                .paths(PathSelectors.ant("/admin/**"))
                .build();
    }

    *//**
     * 配置swagger信息
     * @return
     *//*
    private ApiInfo apinfo(){
        return new ApiInfo("数独对接api","对接后台接口","v1.0","www.ec-sudo.com","","","www.ec-sudo.com");
    }*/
}